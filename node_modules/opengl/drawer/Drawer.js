
class Drawer
{
	constructor(canvas){
		let gl = canvas.getContext("webgl2");
		console.log(gl.getContextAttributes());

		this.width = width;
		this.height = height;

		this.canvas = canvas;
		this.gl = gl;
	}

	draw(scene, camera){

	}
}