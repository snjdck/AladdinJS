"use strict";

const WebGL = require('./WebGL');

class Render
{
	constructor(){
	}

	drawScreen(){
		const {gl} = WebGL;
		gl.drawArrays(gl.TRIANGLE_FAN, 0, 4);
	}
}

module.exports = Render;