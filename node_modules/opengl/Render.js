"use strict";

const WebGL = require('./WebGL');

class Render
{
	constructor(){
	}

	drawScreen(){
		const {gl} = WebGL;
		gl.drawArrays(gl.TRIANGLE_STRIP, 0, 4);
	}
}

module.exports = Render;