"use strict";

const WebGL = require('./WebGL');
const {UniformBuffer} = require("./uniformbuffers");

class Render3D
{
	constructor(){
		let {programMgr} = WebGL;
		this.uniformBuffer = new UniformBuffer(2, 8, programMgr.MAX_VERTEX_UNIFORM_VECTORS);
	}
}

module.exports = Render3D;