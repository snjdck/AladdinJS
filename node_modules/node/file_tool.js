
const {basename, extname} = require('path');
const {renameSync, existsSync, copyFileSync, unlinkSync, statSync} = require('fs');
const {walkDir} = require('./fs');

const extSet = new Set();
walkDir('', path => {
	extSet.add(extname(path));
});
console.log(extSet);
