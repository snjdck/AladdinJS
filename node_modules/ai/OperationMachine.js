
const OperationMachine = desc => text => {
	for(let {test, exec} of desc){
		let args = test.exec(text);
		if(args == null)continue;
		return exec(...args.slice(1));
	}
}

module.exports = OperationMachine;
