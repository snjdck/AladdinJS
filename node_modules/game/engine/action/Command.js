
const Executor = context => actions => {
	const stack = [];
	const mapFn = () => stack.pop();
	for(let i=actions.length-1; i>=0; --i){
		const action = context[actions[i]];
		stack.push(action.exec(...Array.from(action.exec, mapFn)));
	}
	return stack.reverse();
}

exports.Executor = Executor;
