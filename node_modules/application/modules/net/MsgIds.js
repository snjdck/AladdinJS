
const MsgNames = require('./MsgNames');
const protocol = require('./protocol');
const set = require('utils/chain/set');

module.exports = Object.entries(protocol).reduce((array, [k, v]) => set(array, v, MsgNames[k]), []);