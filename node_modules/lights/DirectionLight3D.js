"use strict";

const {DepthRenderTarget} = require("../rendertargets");

class DirectionLight3D
{
	constructor(view3d){
		const {canvas, gl} = view3d;

		this.shadowRenderTarget = new DepthRenderTarget(2048, 2048);
	}

	drawShadowMap(){
		this.shadowRenderTarget.active(gl);

		gl.clear(gl.DEPTH_BUFFER_BIT);
	}

	drawLight(){

	}
}

module.exports = DirectionLight3D;