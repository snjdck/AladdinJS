'use strict';

const {createElement} = require('../createElement');

const Fieldset = createElement(function(tag, end, {title, children, disabled}){
	tag('fieldset', {disabled})
		tag('legend'), tag`${title}`, end('legend')
		tag(React.Fragment, {children}, true)
	end('fieldset')
});

module.exports = Fieldset;
