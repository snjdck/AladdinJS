'use strict';

require('./MenuGroup.css');

const {createElement} = require('../createElement');

const MenuGroup = createElement(function(tag, end, {menuWidth, children}){
	tag('nav', {className:'menu-group', style:{'--menu-width':menuWidth}, children})
	end('nav')
});

module.exports = MenuGroup;
