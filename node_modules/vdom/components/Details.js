'use strict';

const {createElement} = require('../createElement');

const Details = createElement(function(tag, end, {title, children, open}){
	tag('details', {open})
		tag('summary'), tag`${title}`, end('summary')
		tag(React.Fragment, {children}, true)
	end('details')
});

module.exports = Details;
