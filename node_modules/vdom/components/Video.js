
require('./Video.css');

const {createElement} = require('../createElement');

const Video = createElement(function(tag, end){
	tag('div', {className:'video'})
		tag('video', {controls:true}, true)
		tag('canvas', true)
	end('div')
});

module.exports = Video;
