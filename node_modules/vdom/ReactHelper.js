'use strict';

//value, checked
function onInputChange(fn, key='value'){
	return evt => fn(evt.target[key]);
}

function ReactInit(container, ...args){
	ReactDOM.createRoot(container).render(React.createElement(...args));
}

exports.onInputChange = onInputChange;
exports.ReactInit = ReactInit;
