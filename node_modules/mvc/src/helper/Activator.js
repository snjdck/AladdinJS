
class Activator
{
	_roleMap = new Map();

	register(role, injector){
		this._roleMap.set(role, injector);
	}

	activate(){
		for(let [role, injector] of this._roleMap)
			injector.getInstance(role);
		this._roleMap.clear();
	}
}

export default Activator;