
import {Injector} from 'ioc';
import Module from './Module';
import MsgName from './MsgName';

class Application
{
	_injector = new Injector();
	_moduleDict = Object.create(null);

	constructor(){
		this._injector.mapValue(Application, this, null);
		this._injector.mapValue(Injector, this._injector, null);
	}

	regModule(meta){
		let module = new Module(meta, this._injector);
		console.assert(!(this.hasStartup || (module.name in this._moduleDict)));
		this._moduleDict[module.name] = module;
		return this;
	}

	getModule(name){
		return this._moduleDict[name];
	}

	notify(msgName, msgData){
		for(let module of Object.values(this._moduleDict)){
			module.notify(msgName, msgData);
		}
	}

	startup(...args){
		if(this.hasStartup)return;
		Object.defineProperty(this, "hasStartup", {value:true});
		this.onStartup(...args);
		this.notify(MsgName.ModuleStartup);
	}

	onStartup(){
		let moduleList = Array.from(Object.values(this._moduleDict));
		for(let module of moduleList) initAllModels(module);
		for(let module of moduleList) module.activateRoles();
		for(let module of moduleList) initAllServices(module);
		for(let module of moduleList) module.activateRoles();
		for(let module of moduleList) initAllControllers(module);
		for(let module of moduleList) initAllCommands(module);
		for(let module of moduleList) module.onStartup();
	}
}

function initAllModels(module){
	for(let v of module.collectAllModels()){
		if(Array.isArray(v)){
			if(v.length == 2){
				module.regModel(v[1], v[0]);
				continue;
			}else if(v.length == 1){
				v = v[0];
			}else{
				throw new Error(v);
			}
		}
		if(v.prototype){
			module.regModel(new v());
		}else{
			module.regModel(v);
		}
	}
}

function initAllServices(module){
	for(let v of module.collectAllServices()){
		if(!Array.isArray(v)){
			module.regService(v, v);
		}else if(v.length == 3){
			module.regService(...v);
		}else if(v.length == 2){
			if(typeof v[1] == "boolean"){
				module.regService(v[0], ...v);
			}else{
				module.regService(...v);
			}
		}else if(v.length == 1){
			module.regService(v[0], v[0]);
		}else{
			throw new Error(v);
		}
	}
}

function initAllControllers(module){
	for(let v of module.collectAllControllers()){
		module.regController(v);
	}
}

function initAllCommands(module){
	for(let [k, v] in Object.entries(module.collectAllCommands())){
		module.regCommand(k, v);
	}
}

export default Application;
