
export default module => next => (name, data) => {
	console.groupCollapsed('%cmodule.notify(%s, %o)', 'color: red;', name.valueOf(), data);
	let result = next(name, data);
	console.groupEnd();
	return result;
}