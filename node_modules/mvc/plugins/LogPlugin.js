
export default module => next => msg => {
	let {name, data} = msg;
	console.groupCollapsed('%c%s.notify(%s, %o)', 'color: blue;', module.name, name.valueOf(), data);
	let result = next(msg);
	console.groupEnd();
	return result;
}