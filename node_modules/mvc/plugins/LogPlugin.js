
export default module => next => msg => {
	let {name, data, from} = msg;
	console.groupCollapsed('%c%s/%s.notify(%s, %o)', 'color: blue;', module.name, from, name.valueOf(), data);
	let result = next(msg);
	console.groupEnd();
	return result;
}