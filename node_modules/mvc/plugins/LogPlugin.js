
export default module => next => (name, data) => {
	console.groupCollapsed('%c%s.notify(%s, %o)', 'color: blue;', module.name, name.valueOf(), data);
	let result = next(name, data);
	console.groupEnd();
	return result;
}