'use strict';

module.exports = class {
	constructor(realInjector, klass){
		this.realInjector = realInjector;
		this.klass = klass;
	}
	getValue(){
		return this.realInjector.inject(this.klass());
	}
}
