
class MinorRule
{
	getValue(injector, key){
		let [type, id] = key;
		let rule = injector.getRule([type, 'minor']);
		return rule && rule.getValue(injector, key);
	}
}

export default MinorRule;