
class CoverRule
{
	getValue(self, key){
		let _key = [key[0], 'cover'];
		let rule;
		for(let injector=self; injector; injector=injector.parent){
			rule = injector.getRule(_key, false) || rule;
		}
		return rule && rule.getValue(self, key);
	}
}

export default CoverRule;