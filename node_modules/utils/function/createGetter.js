function createGetter(factory){
	const dict = Object.create(null);
	return function(port, createIfNotExist=true){
		if(port in dict)return dict[port];
		if(!createIfNotExist)return;
		return dict[port] = factory();
	}
}

module.exports = createGetter;