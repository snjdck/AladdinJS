'use strict';

const identity = arg => arg;
const reduceFn = (a, b) => (...args) => a(b(...args));

function compose(...fnList){
	if(fnList.length === 0)return identity;
	return fnList.reduce(reduceFn);
}

module.exports = compose;
