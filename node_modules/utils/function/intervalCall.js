
const {timeline} = document;

const intervalCall = (fn, duration=1000, timestamp=0) => function(){
	const now = timeline.currentTime;
	if(now < timestamp)return;
	timestamp = now + duration;
	fn.apply(this);
}

exports.intervalCall = intervalCall;