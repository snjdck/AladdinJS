'use strict';

const call = Function.prototype.apply.call.bind(Function.prototype.apply);

module.exports = (fn, delay=0, timer) => function(...args){
	clearTimeout(timer);
	timer = setTimeout(call, delay, fn, this, args);
}
