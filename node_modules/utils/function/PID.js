/*
error = 目标值 - 测量值
*/
const PID = (kp, ki, kd) => {
	let lastError = 0;
	let integral = 0;
	return error => {
		integral += error;
		const derivative = error - lastError;
		lastError = error;
		return kp * error + ki * integral + kd * derivative;
	}
}