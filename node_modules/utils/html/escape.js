'use strict';

const dict = {
	'<': '&lt;',
	'>': '&gt;',
	'&': '&amp;',
};

const pattern = new RegExp('[' + Object.keys(dict).join('') + ']', 'g');
const _replace = Reflect.get.bind(null, dict);

module.exports = v => v.replace(pattern, _replace);
