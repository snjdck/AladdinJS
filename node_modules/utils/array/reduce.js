
function* calcOffset(list){
	let i = 0;
	for(let v of list){
		yield i;
		i += v.length;
	}
}

function* calcSize(list){
	let i = 0;
	for(let v of list){
		i += v.length;
		yield i;
	}
}

exports.calcOffset = v => Array.from(calcOffset(v));
exports.calcSize = v => Array.from(calcSize(v));
