
function merge(length, ...args){
	const size = args.map(v => v.length / length);
	return Array.from({length}, (_, i) => args.map((v, j) => v.slice(i*size[j], (i+1)*size[j]))).flat(2);
}

exports.merge = merge;
//console.log(merge(2, [1,2,3,6,7,8], [4,5,9,10]))