const {mapN} = require('./everyN');
const {zip} = require('./zip');

function merge(length, ...args){
	return zip(...args.map(v => mapN(v, v.length / length))).flat(2);
}

exports.merge = merge;
//console.log(merge(2, [1,2,3,6,7,8], [4,5,9,10]))