'use strict';

function invertMask(value, mask){
	return (value & ~mask) | (~value & mask);
}

function invertMap(map, count, x, y, isY){
	if(isY){
		for(let i=0; i<count;++i){
			map[y+i] = invertMask(map[y+i], 1 << x);
		}
	}else{
		map[y] = invertMask(map[y], ((1 << count) - 1) << x);
	}
}

