'use strict';

function unique(list, keyFn=null){
	if(!keyFn)return Array.from(new Set(list));
	return list.filter(function(item){
		const key = keyFn(item);
		if(this.has(key))return false;
		this.add(key);
		return true;
	}, new Set());
}

module.exports = unique;
