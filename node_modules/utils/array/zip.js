
const zip = f => (...args) => Array.from({length: args.length < 1 ? 0 : f(...args.map(v => v.length))}, (_, i) => args.map(v => v[i]));

exports.zip = zip(Math.min);
exports.zip_longest = zip(Math.max);
