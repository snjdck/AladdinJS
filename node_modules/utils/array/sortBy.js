'use strict';

const sortBy = function(){
	const _sort = (a, b) => a[1] - b[1];
	const _map = v => v[0];
	return (list, f, indexFlag=false) => list.map((v, i) => [indexFlag ? i : v, f(v, i, list)]).sort(_sort).map(_map);
}();

module.exports = sortBy;
