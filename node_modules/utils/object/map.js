
function map(fn, thisArg)
{
	const result = {};
	for(let key in this){
		let val = this[key];
		[key, val] = fn.call(thisArg, key, val, this);
		result[key] = val;
	}
	return result;
}

export default map;