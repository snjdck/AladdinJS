
const IndexDistributor = (map=new Map()) => key => {
	if(key === undefined)return map.clear();
	if(map.has(key))return map.get(key);
	const index = map.size;
	map.set(key, index);
	return index;
}

module.exports = IndexDistributor;
