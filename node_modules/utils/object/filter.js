
function filter(fn, thisArg)
{
	const result = {};
	for(let key in this){
		let val = this[key];
		if(fn.call(thisArg, key, val, this)){
			result[key] = val;
		}
	}
	return result;
}

export default filter;