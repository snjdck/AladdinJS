
function filter(fn, thisArg)
{
	const result = {};
	for(let key in this){
		let val = this[key];
		if(fn.call(thisArg, val, key, this)){
			result[key] = val;
		}
	}
	return result;
}

export default filter;