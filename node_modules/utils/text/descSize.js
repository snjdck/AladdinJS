
const descSize = function(){
	const level = [[2**30, 'G'], [2**20, 'M'], [2**10, 'K']];
	return value => {
		for(let [t, u] of level){
			if(value < t)continue;
			return Math.floor(100 * value / t) * 0.01 + u;
		}
		return value + 'B';
	}
}();

module.exports = descSize;
