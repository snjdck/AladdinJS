
function concat(a, b, limit=4096){
	if(b.length > limit)return b.slice(-limit);
	if(b.length == limit)return b;
	if(b.length + a.length <= limit)return a + b;
	return a.slice(b.length - limit) + b;
}

module.exports = concat;
