
const countdown = (updateFn, total=60000, interval=1000) => new Promise(resolve => function onTimer(left){
	if(left <= 0)return resolve();
	updateFn(left);
	setTimeout(onTimer, interval, left - interval);
}(total);

exports.countdown = countdown;
