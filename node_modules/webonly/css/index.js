'use strict';

/*
'beforebegin'：元素自身的前面。
'afterbegin'：插入元素内部的第一个子节点之前。
'beforeend'：插入元素内部的最后一个子节点之后。
'afterend'：元素自身的后面。
element.insertAdjacentElement(position, element);
element.insertAdjacentHTML(position, text);
element.insertAdjacentText(position, text);

innerHTML
innerText
textContent
*/

/*
let styleSheet = document.styleSheets[0];
let rule = styleSheet.cssRules[0];
rule.style === element.style;
styleSheet.insertRule()
*/

function createStyle(info, prettyFlag){
	return '{' + Object.entries(info).map(([k, v]) => `${k}:${v};`).join('') + '}';
}

function appendStyleSheetToDocument(text){
	//style.sheet.insertRule('.red::before{color:green}');
	let style = document.createElement('style');
	style.appendChild(document.createTextNode(text));
	document.head.appendChild(style);
}