
const Arduino = require('./Arduino');
const ArduinoScratchCode = require('./ArduinoScratchCode');
const ArduinoExternalCode = require('./ArduinoExternalCode');

const Python = require('./Python');
const PythonScratchCode = require('./PythonScratchCode');
const PythonExternalCode = require('./PythonExternalCode');

const ScratchCode = require('./ScratchCode');

/*
const castType = type => {
	if(type === 'argument_reporter_string_number')
		return 'double';
	if(type === 'argument_reporter_boolean')
		return 'bool';
	return 'int';
}

const castRetType = (type, defaultType) => {
	switch(type){
		case "CHARACTER": return 'char';
		case "TEXT": return 'String';
		case "BOOLEAN": return 'bool';
		case "SHORT_NUMBER": return 'short';
		case "NUMBER": return 'int';
		case "LARGE_NUMBER": return 'long';
		case "DECIMAL": return 'double';
	}
	return defaultType;
}
*/
const Blockly = {
	//Arduino: Object.assign(new Arduino(), {context: Object.assign({}, ScratchCode, ArduinoScratchCode, ArduinoExternalCode)}),
	//Python: Object.assign(new Python(), {context: Object.assign({}, ScratchCode, PythonScratchCode, PythonExternalCode)}),
	Arduino: Object.assign(new Python(), {context: Object.assign({}, ScratchCode, PythonScratchCode, ArduinoExternalCode)}),
}

module.exports = Blockly;