'use strict';

const escape = require('utils/html/escape');

module.exports = function(element, code){
	code = escape(code).replace(/\t/g, ' '.repeat(4));
	let list = code.trim().split('\n').map(v => `<code>${v}</code>`);
	element.innerHTML = list.join('');
}
