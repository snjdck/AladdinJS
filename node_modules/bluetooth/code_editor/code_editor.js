'use strict';

module.exports = function(element, code){
	code = code.replace(/\t/g, ' '.repeat(4));
	let list = code.trim().split('\n').map(v => `<span>${v}</span>`);
	element.innerHTML = list.join('');
	element.style.setProperty('--pad', Math.floor(Math.log10(list.length)) + 1);
}
