
module.exports = (selector, factory) => engine => {
	let entities = engine.findEntities(selector);
	let updater = factory.call(engine, entities);
	return () => entities.forEach(updater, engine);
}