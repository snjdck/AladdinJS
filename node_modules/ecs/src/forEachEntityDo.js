
function forEachEntityDo(entitySet, onAdded, onRemoved){
	entitySet.forEach(onAdded);
	if(onAdded)
		entitySet.events.on('added', onAdded);
	if(onRemoved)
		entitySet.events.on('removed', onRemoved);
}

module.exports = forEachEntityDo;