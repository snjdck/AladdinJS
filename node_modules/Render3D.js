"use strict";

const {UniformBuffer} = require("./uniformbuffers");

class Render3D
{
	constructor(gl){
		this.uniformBuffer = new UniformBuffer(2, 7, gl.programMgr.MAX_VERTEX_UNIFORM_VECTORS);
	}
}

module.exports = Render3D;