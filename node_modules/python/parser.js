"use strict";

const fs = require("fs");
const child_process = require("child_process");

function parse(path){
	let data = child_process.execSync(`python parser.py ${path}`);
	return JSON.parse(data.toString());
}

module.exports = {
	parse
};