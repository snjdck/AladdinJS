"use strict";

const {loadData} = require("../utils/loader");

class AssetManager{
	constructor(){
	}

	loadFiles(fileList){
		return Promise.all(fileList.map(this.loadFile));
	}

	loadFile(file){
		return loadData(`/assets/${file}.bmd.mesh`, "arraybuffer");
	}

	loadImage(name){

	}
}

module.exports = AssetManager;
