
const os = require('os');

module.exports = function(){
	let list = Object.values(os.networkInterfaces())
	.reduce((a, b) => a.concat(b))
	.filter(({family, internal}) => family == 'IPv4' && !internal);
	if(list.length > 0){
		return list[0].address;
	}
}
