'use strict';

const {HttpServer, StaticFile} = require('./HttpServer');

const path = '';

HttpServer({}, [StaticFile(path)]);

const thunder = path => path.startsWith('thunder://') ? Buffer.from(path.slice(10), 'base64').toString().slice(2, -2) : path;
