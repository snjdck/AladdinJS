'use strict';

const {writeFileSync} = require('fs');
const {basename, join} = require('path');
const load = require('../net/load');

function download(list, output=''){
	list.forEach(url => load(url).then(data => writeFileSync(join(output, basename(url)), data)));
}


module.exports = download;
