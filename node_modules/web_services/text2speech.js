
const load = require('../net/load');

const link = 'https://synthesis-service.scratch.mit.edu/synth';

function text2speech(text){
	return load(link, {query:{locale:'en-US',gender:'female',text},responseType:'audio'});
}

module.exports = text2speech;

