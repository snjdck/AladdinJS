
const load = require('../net/load');

const link = 'https://translate-service.scratch.mit.edu/translate';

function translate(language, text){
	return load(link, {query:{language,text}});
}

translate('zh-cn', 'hello').then(console.log)

//module.exports = translate;

