'use strict';

const TextureMaterial = require('./TextureMaterial');
const Pipeline = require('./Pipeline');

const pipeline = new Pipeline({
	...TextureMaterial.pipeline.desc,
	fragment:{
		code: TextureMaterial.pipeline.desc.fragment.code,
		targets:[{blend: 'src-alpha add one-minus-src-alpha'}]
	},
	depthStencil:{depthWriteEnabled: false}
});

class AlphaTextureMaterial extends TextureMaterial.Subclass(pipeline){
	static order = 1;
}

module.exports = AlphaTextureMaterial;
