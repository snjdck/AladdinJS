'use strict';

const Vulkan = require('../../Vulkan');
const UniformBufferSlotCache = require('../../helper/UniformBufferSlotCache');

module.exports = new UniformBufferSlotCache(
	'4',
	256,
	({material}) => new Float32Array(material.color)
);
