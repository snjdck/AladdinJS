'use strict';

const Vulkan = require('../../Vulkan');
const {UniformBufferSlot} = require('../../helper/Slot');

module.exports = UniformBufferSlot(
	'4',
	256,
	({material}) => new Float32Array(material.color)
);
