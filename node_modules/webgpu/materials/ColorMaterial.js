'use strict';

const Vulkan = require('../Vulkan');
const Material = require('./Material');

const pipelineDesc = {
	layout: ['0', '2_dynamic', '2_dynamic', '4'],
	vertex: {
		module: 'vertex_3d',
		buffers: [{
			arrayStride: 9 << 2,
			stepMode: 'vertex',
			attributes: ['float32x3', 'float32x3', 'float32x2', 'uint32']
		}]
	},
	fragment: {
		module: 'fragment_3d_color',
		targets:[{
			format: 'bgra8unorm',
			blend: ['transparent']
		}]
	},
	depthStencil: {
		format: 'depth24plus-stencil8',
		depthWriteEnabled: true,
		depthCompare: 'less-equal',
	}
};

class ColorMaterial extends Material.Subclass(
	pipelineDesc,
	{transform:1,bone:2,color:3}
){
	static onDraw(instanceCount, mesh){
		Vulkan.renderPassEncoder.draw(mesh.vertexCount, instanceCount);
	}
	
	constructor(color){
		super();
		this.color = color;
	}
}

module.exports = ColorMaterial;
