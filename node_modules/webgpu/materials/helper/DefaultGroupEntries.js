
const maxCountPerDraw = 100;

module.exports = {
	maxCountPerDraw,
	worldMatrixList: function(){
		const temp = {stride: 12, mapFn: v => v.entity.worldTransform.rawData};
		return () => ({name:'worldMatrixList', type:`array<mat3x4<f32>, ${maxCountPerDraw}>`, buffer:{}, temp});
	}(),
	animationTextureInfo: function(){
		const temp = {stride: 4, mapFn: v => [v.animationTime ?? 0, v.animationIndex ?? 0]};
		return () => ({name:'animationTextureInfo', type:`array<vec4<f32>, ${maxCountPerDraw}>`, buffer:{}, temp});
	}(),
	animationTexture: function(){
		return {name:'animationTexture', texture:{viewDimension: '2d-array', sampleType:'unfilterable-float'}};
	},
};
