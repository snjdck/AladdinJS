'use strict';

class IndexDistributor
{
	constructor(){
		this.list = [];
	}

	getIndex(key){
		const {list} = this;
		const index = list.indexOf(key);
		return index >= 0 ? index : list.push(key) - 1;
	}

	reset(){
		this.list.length = 0;
	}
}

module.exports = IndexDistributor;
